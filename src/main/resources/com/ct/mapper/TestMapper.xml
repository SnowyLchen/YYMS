<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ct.mapper.TestMapper">
<!--
  <resultMap id="AirMap" type="com.cj.pojo.Air4010">
    <id column="id" jdbcType="INTEGER" property="Id" />
    <result column="air_name" jdbcType="INTEGER" property="AirName" />
  </resultMap>


  <select id="seeAll"  resultMap="AirMap">
    select * from air20174010
  </select>

  <select id="place"  resultMap="AirMap">
    select s_city,e_city from air20174010
  </select>

  <select id="queryByPP"  resultMap="AirMap">
    select * from air20174010
    <where>
      <if test="param1!=null and param1 !='' and param1 !='-1'">
          s_city=#{param1}
      </if>
      <if test="param2!=null and param2 !='' and param2 !='-1'">
          and e_city=#{param2}
      </if>
    </where>
  </select>

  <select id="queryByTime"  resultMap="AirMap">
    select * from air20174010
    <where>
      <if test="param1!=null and param1 !='' and param1 !='-1'">
          s_city=#{param1}
      </if>
      <if test="param2!=null and param2 !='' and param2 !='-1'">
          and e_city=#{param2}
      </if>
      order by alltime desc
    </where>
  </select>
  <select id="queryByPrice"  resultMap="AirMap">
    select * from air20174010
    <where>
      <if test="param1!=null and param1 !='' and param1 !='-1'">
          s_city=#{param1}
      </if>
      <if test="param2!=null and param2 !='' and param2 !='-1'">
          and e_city=#{param2}
      </if>
      order by air_price asc
    </where>
  </select>-->
</mapper>